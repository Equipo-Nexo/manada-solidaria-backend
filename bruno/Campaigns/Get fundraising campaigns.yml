info:
  name: Get fundraising campaigns
  type: http
  seq: 5

http:
  method: GET
  url: "{{MANADA_SOLIDARIA_HOST}}/campaigns/fundraising_campaigns"
  params:
    - name: type
      value: NEWS
      type: query
      disabled: true
    - name: type
      value: DONATION
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
