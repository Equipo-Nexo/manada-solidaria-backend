info:
  name: Get campaigns
  type: http
  seq: 4

http:
  method: GET
  url: "{{MANADA_SOLIDARIA_HOST}}/campaigns"
  params:
    - name: type
      value: NEWS
      type: query
      disabled: true
    - name: type
      value: DONATION
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
