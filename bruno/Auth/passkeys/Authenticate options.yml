info:
  name: Authenticate options
  type: http
  seq: 2

http:
  method: POST
  url: "{{MANADA_SOLIDARIA_HOST}}/webauthn/authenticate/options"
  body:
    type: json
    data: |-
      {
        "username": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
