info:
  name: Register options
  type: http
  seq: 1

http:
  method: POST
  url: "{{MANADA_SOLIDARIA_HOST}}/webauthn/register/options"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
